name: GitHub Actions Demo

on: [pull_request]
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: |
         echo "hello world"
      - name: This will fail
        run : exit 1;
  
      
   
